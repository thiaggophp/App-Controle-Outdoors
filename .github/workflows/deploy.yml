name: Deploy VPS

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy no VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: root
          key: ${{ secrets.VPS_SSH_KEY }}
          script: |
            cd /var/www/outdoor-controle
            git fetch origin
            git reset --hard origin/main
            npm install
            npm run build
            BUILD_TS=$(date +%s)
            sed -i "s/const CACHE=\"[^\"]*\"/const CACHE=\"outdoor-${BUILD_TS}\"/" dist/sw.js
            cp -r dist/* /var/www/outdoor-html/
            systemctl reload nginx
            echo "Deploy OutdoorControle concluído! Cache SW: outdoor-${BUILD_TS}"
